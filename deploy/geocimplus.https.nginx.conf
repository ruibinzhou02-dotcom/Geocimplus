# Inspect the actual Nginx installation and back up its site configuration before enabling.
server {
    listen 443 ssl http2;
    ssl_certificate /etc/letsencrypt/live/geocimplus.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/geocimplus.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:GeoCIM:1m;
    server_name geocimplus.com www.geocimplus.com;
    root /www/wwwroot/geocimplus/current;
    index index.html;
    server_tokens off;
    gzip on;
    gzip_types application/javascript application/json text/css;
    gzip_vary on;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy no-referrer always;
    add_header X-Frame-Options SAMEORIGIN always;
    location ^~ /.well-known/acme-challenge/ { root /www/wwwroot/geocimplus/acme; }
    location ~ /\. { deny all; }
    location /api/ {
        limit_except GET { deny all; }
        default_type application/json;
        add_header Cache-Control no-store;
        try_files $uri =404;
    }
    location /assets/ { try_files $uri =404; expires 7d; }
    location = /index.html { add_header Cache-Control no-cache; }
    location / { try_files $uri $uri/ =404; }
}

server {
    listen 80;
    server_name geocimplus.com www.geocimplus.com;
    location ^~ /.well-known/acme-challenge/ { root /www/wwwroot/geocimplus/acme; }
    location / { return 301 https://geocimplus.com$request_uri; }
}
