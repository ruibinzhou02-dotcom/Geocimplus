GeoCIM+ 交付说明（V1）

版本日期：2026-09-11
分支：main
提交：81e00ca
目标：将 GeoCIM+ 打造成可展示的轻量 GIS 分析前端，支持本地工作流优先。

## 1. 代码仓库状态

### 已完成（已入库）
- 模型建构器（Circuit）落地为可浮动窗口，支持拖拽与高度调节
- 图层与属性表可视化链路：图层切换、透明度、选择、筛选、统计、截图导出
- 本地文件导入与解析：GeoJSON / Shapefile（含 ZIP 套件）/ GeoTIFF
- 中英文界面切换（默认 English）
- 对话功能（基础版、离线/规则化可扩展方向已保留）
- 示例数据与配置脚本：项目在本地运行可复现
- 部署脚本与静态发布链条：`build_public_analysis.py`、`verify_public.py`、`deploy_static.sh`

### 说明中的边界
- 真实数据导入与分析以本地会话为主，默认不在云端长期持久化新用户数据
- 示例数据与界面可视化为主，复杂 AI 自动分析（如完整大模型工具调用）尚未作为默认生产链路
- 沙盒组件与 API 深度执行目前为 V2 目标，当前版本以安全规则/模板流程为主

## 2. 本地资源与路径

### 代码与运行
- 仓库根：`D:\GeoCIM_Demo`
- 前端源码：`src/*`
- 文档：`docs/*`
- 脚本：`scripts/*`
- 配置：`config/*`（`private` 相关会被忽略）

### 数据与产物（建议）
- 真实原始数据建议放在项目外只读目录，当前项目保留本地审计副本目录：`data/`
- 测试报告与导出快照：`reports/`
- 本地发布版本：`releases/`（已列入 ignore，不建议上传）
- 个人环境：`geocim-dev/`（Python venv）

## 3. 本次项目目标回顾

1. 形成可演示的 GIS 分析前端（地图、图层、属性、导出）
2. 支持基础的模型建构器闭环（节点编辑、运行、结果回写）
3. 建立一套本地为主的处理策略：用户上传和分析不默认写入云端
4. 支持后续 V2 无缝接入（场景首页、离线 NLP、沙盒组件、EPW 输入）

## 4. 线上部署

- 当前站点地址：`https://geocimplus.com/`
- Nginx 站点/证书配置和部署脚本已整理在仓库与运维文档里
- 站点更新方式：
  - 本地构建/打包
  - 上传发布包到服务器
  - `deploy_static.sh` 原子替换 `current` 指向
- 回滚建议保留前一版 release 目录，并在必要时回指 `current`

## 5. 已知限制

- 复杂模型执行（如高级气候/热舒适算法）尚未默认内置
- 部分外部 API 与高级编译组件仍按 V2 任务推进
- Windows EXE 路线未作为本次交付标准产物

## 6. 下一步推荐（V2）

- 场景首页（数字孪生、低空经济、交通、热环境等）
- EPW + raster-to-vector 的热环境样本链路
- 离线 NLP 命令集（“打开图层”“筛选”“运行流程”）
- 模型组件分组（GH 思路）与沙盒节点治理
- 片区级数据入口与“可复用流程模板市场”

## 7. 维护接手备注

- 推送方式：`git push -u origin main`
- 未来版本开发尽量保持“`private`、原始数据、密钥不入库”
- 每次发布前建议先执行 `pnpm build` 与本地 smoke test

