# V2 主入口发布记录

日期：2026-09-21。发布编号：`20260921-111651-v22`。

此前 V2 已发布到 `/v2.html`，但主域名仍加载 V1 的 `index.html`。这次修正正式发布包：V2 同时成为 `/`、`/index.html` 与 `/v2.html` 的页面；V1 移到独立 `/v1.html` 入口。既有 GIS 数据、分析功能和三维渲染逻辑没有改动。

## 访问

- 正式 V2：https://geocimplus.com/
- V2 兼容入口：https://geocimplus.com/v2.html
- 保留 V1：https://geocimplus.com/v1.html
- 本地 V2：http://127.0.0.1:8766/v2.html

本地源代码仍保持 V1 的 `index.html` 和 V2 的 `v2.html` 两个开发入口。`scripts/package_v22.py` 在打包时完成上述公开路径映射；今后沿用同一脚本发布，不会恢复旧版首页。HTML 使用 `no-cache` 重新验证策略，带哈希资源继续缓存。如果已打开的旧页面没有变化，刷新一次；必要时在 Chrome/Edge 按 Ctrl+F5。

## 本次验证

| 检查 | 结果 |
| --- | --- |
| 本机构建 | 成功；V2 主脚本仍为 `v2-DbMo4ErP.js`，功能代码与前一版一致 |
| 首页、index、V2 HTML | HTTPS 200，三者 SHA-256 完全一致，Cache-Control 为 no-cache |
| www 主域名与 HTTP 入口 | www 返回同一 V2 页面，HTTP 跳转 HTTPS |
| JS/CSS | HTML 引用的资源全部返回 200，内容类型正确 |
| 固定示例计算服务 | 健康检查 ready，server 模式，服务 active |
| 主域名浏览器操作 | V2 三卡片首页；城市更新载入 17 个图层，服务器返回计算结果（本次约 4.8 秒）；三维画面可见 |
| 中英文和流程面板 | 切换正常，Circuit 菜单、节点、端口与数字滑条显示中文 |
| 新建项目 | Local analysis，0 个图层，固定示例云模式不可选，输入/输出位置入口存在 |
| 刷新 | 主域名刷新后仍为 V2 首页 |
| 旧版保留入口 | V1 地图与原有 5306 栋建筑统计成功加载 |
| 浏览器错误日志 | 本轮检查未见 error / warn |

此次只修改发布入口和文档，未重复完整功能回归。之前路网版本的 62 项自动测试结果见 `reports/v2.2/road-relief-tests.txt`。上述检查不等同于所有数据格式、全部交互和导出功能的再验收，下一步由用户使用真实操作测试 V2。

详细 HTTP 检查：`reports/v2.2/homepage-verification.json`。发布元信息：`releases/v22-release.json`。这些目录不提交 GitHub。

## 回滚

部署前站点与计算服务均为 `20260921-101548-v22`，当前均为 `20260921-111651-v22`。备份位于服务器 `/var/backups/geocim/20260921-111651-v22/`，保存上一版指针、Nginx 配置和服务单元。站点使用版本目录及原子切换，旧版文件没有删除。

需要恢复此次发布之前的状态时，在项目 PowerShell 中使用已配置的项目 SSH 密钥：

```powershell
Set-Location D:\GeoCIM_Demo
scp -i private/deploy_ed25519 -o UserKnownHostsFile=private/known_hosts -o StrictHostKeyChecking=yes scripts/rollback_v22.sh admin@47.250.162.189:/tmp/rollback_v22.sh
ssh -i private/deploy_ed25519 -o UserKnownHostsFile=private/known_hosts -o StrictHostKeyChecking=yes admin@47.250.162.189 'sudo -n bash /tmp/rollback_v22.sh 20260921-111651-v22'
```

这会恢复“根首页 V1、`/v2.html` 为 V2”的上一部署状态。只有确实需要撤销这次首页切换时才执行。本轮没有为了验证而回滚正在使用的网站。私钥仍只保存在本机 `private` 目录，不应复制到网站、GitHub 或聊天中。

## 下一步

V3 仅建立 [待确认开发清单](V3开发清单待确认.md)，包括 Circuit 输入与分组、连接反馈、坐标、沙盒、结果溯源、批量、渲染和气象插件等候选项目。按用户最新决定，先测试 V2，再核定 V3 开发范围；本轮没有把计划功能混入线上版本。
